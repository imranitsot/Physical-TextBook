# Plan: Premium UI Overhaul

## 1. CSS Customization
-   [x] Modify `src/css/custom.css`.
-   [x] specific `:root` variables for Neon Blue palette.
-   [x] Add `@keyframes gradient-move` for hero animation.
-   [x] Style `.feature-card` with backdrop-filter (glassmorphism).

## 2. Homepage Architecture
-   [x] Rewrite `src/pages/index.js`.
-   [x] Create `pillars` array with metadata for:
    -   Nervous System
    -   The Body
    -   The Brain
    -   The Digital Twin

## 3. Branding
-   [x] Generate `logo.png` and `favicon.ico`.
-   [x] Update `docusaurus.config.js` `navbar.logo`.
